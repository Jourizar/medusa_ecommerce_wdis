{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/medusa-backend && npm install --legacy-peer-deps && npm run build"
  },
  "deploy": {
    "startCommand": "cd apps/medusa-backend && npx medusa db:migrate && npx medusa start",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
